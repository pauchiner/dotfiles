# ALLOWS HOMEBREW APPS TO RUN
set PATH $PATH:/opt/homebrew/bin/

# ANDROID STUDIO
set ANDROID_SDK_ROOT $HOME/Library/Android/sdk
set PATH $PATH:$ANDROID_SDK_ROOT/emulator
set PATH $PATH:$ANDROID_SDK_ROOT/platform-tools
